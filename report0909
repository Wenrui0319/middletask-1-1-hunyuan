 InteractiveEdit-3D 的交互式系统论文框架

### 1. 引言
- 介绍了 3D 内容创建领域的变化，以及现有生成模型（如 Hunyuan3D、Magic3D 和 DreamFusion）的局限性，特别是在用户控制方面。
- 提出了 InteractiveEdit-3D 系统，该系统通过用户交互来引导 3D 生成流程。

### 2. 相关工作
- 讨论了与交互式分割和图像编辑相关的工作。包括Qwen-inpainting部分【王雯睿】
- comfyui工作流部分，包括Qwen-Image-Edit【罗雅淇】
### 3. InteractiveEdit-3D 系统【罗雅淇完善】
- 系统的核心是一个交互循环：指定（Specify）→ 编辑（Edit）→ 生成（Generate）。

#### 3.1 通过交互式分割进行区域指定
- 用户通过 Gradio 界面上传图像，并与图像交互以定义感兴趣区域（ROI）。
- 用户可以通过点击目标对象或背景来提供提示，或者绘制边界框。
- 后端处理：图像和用户提示被发送到预加载的 SAM 模型，生成候选掩码，并选择得分最高的掩码。
- 输出：选定的掩码叠加在图像上，提供即时视觉反馈，用户可以迭代添加更多点来细化掩码。

#### 3.2 多模态区域编辑
- 用户可以选择编辑已定义的 ROI，这对于解决歧义、克服遮挡或发挥创意自由至关重要。
- 使用 Qwen-Image-Edit 和 Qwen-Inpainting 模型进行全局和局部编辑。
- Gemini API 作为创意助手，帮助用户以自然语言描述编辑需求。

#### 3.3 区域 3D 生成
- 最终编辑后的图像被传递给 Hunyuan3D 2.1 模型进行 3D 生成。
- 尽管 Hunyuan3D 当前版本不是为区域生成而设计的，但通过用户提供的已隔离和优化的 ROI 图像，引导模型专注于特定内容。

#### 3.4 系统实现和用户界面
- 系统作为 Gradio 应用程序实现，具有快速原型制作能力和基于 Python 的堆栈。
- 用户界面分为两个主要面板：文件系统和功能标签。
- 异步执行 Hunyuan3D 生成任务，提高用户体验。

### 4. 实验【葛俊辰】
- 通过各种用例展示系统的有效性。
- 首先进行定义基线模型，其次逐步移除或修改组件，再然后评估性能，结果比较

完整系统：包含所有三个模块。
基线 A：直接从原始图像生成三维模型，不使用分割和编辑模块。
基线 B：使用 SAM 分割模块，但不进行编辑，直接将分割后的图像输入到 Hunyuan3D。

评估：

- 定量评估：使用生成质量指标（如 FID、SSIM 等）评估每个版本的性能。
- 定性评估：通过视觉对比，展示每个版本生成的三维模型的效果。

结果分析：

- 如果完整系统的生成质量显著高于基线 A 和基线 B，这说明交互式分割和多模态编辑模块对提高生成质量有重要贡献。
- 如果基线 A 的生成质量较差，这说明直接从原始图像生成三维模型是不够的。
- 如果基线 B 的生成质量比基线 A 好但比完整系统差，这说明分割模块有助于提高生成质量，但编辑模块进一步优化了结果。
### 5. 结论
- 提出了 InteractiveEdit-3D 系统，它通过整合 SAM、Qwen 模型和 Hunyuan3D，实现了用户精确指定和编辑 2D 图像区域以进行针对性的 3D 生成。
- 讨论了系统的局限性和未来的工作方向。

### 参考文献
- 文档最后列出了参考文献。

这个框架提供了一个交互式 3D 内容创建系统的全面概述，从用户交互到后端处理，再到最终的 3D 资产生成。
